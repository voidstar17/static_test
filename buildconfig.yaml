apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  name: static_test
  labels:
    app: static_test_app
spec:
  source:
    type: Git
    git:
      uri: https://github.com/voidstar17/static_test
    contextDir: /
  strategy:
    type: Docker                      
    dockerStrategy:
      dockerfilePath: Dockerfile    # Look for Dockerfile in: gitUri/contextDir/dockerfilePath
  output:
    to:
      kind: ImageStreamTag
      name: static_test:latest
